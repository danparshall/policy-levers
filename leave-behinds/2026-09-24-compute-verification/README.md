# 2026-09-24 compute-verification leave-behind — source

Edit `leave-behind.html`, then from this directory:

    python3 render.py        # writes leave-behind.pdf here; needs playwright + chromium
    # (pip install playwright && playwright install chromium)

Copy the result to `../2026-09-24-compute-verification-leave-behind.pdf` when final.
Figures are the two Glass Perimeter diagrams from canaryinstitute.ai/blog/cant-trust-then-verify.
Yellow-highlighted (`class="ph"`) spans are placeholders still to fill.
